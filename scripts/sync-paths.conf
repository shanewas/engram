# engram sync allowlist — the ONLY paths the sync scripts ever auto-commit.
# One path per line, relative to the repo root; '#' starts a comment.
# This file is code, not memory: changing it requires a deliberate manual
# commit (it never syncs automatically). See docs/sync-contract.md §3.
# If this file is missing or empty, sync falls back to this same default list.
index.md
projects
global
inbox
archive
